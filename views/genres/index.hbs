{{! Auhor: Karnasinh Gohil }}

{{! This is heading tage taking title variable value in it }}
<h1 class="margin32px">{{title}}</h1>
{{! Add genre link only be appeared if user is already logged in }}
{{#if user}}
  <a href="/genres/add">Add a new Genre</a>
  <p></p>
{{/if}}
{{! This is the genre list table }}
<table class="table table-success table-striped table-hover">
  <thead>
    <th class="fs-3">Genre Type</th>
  </thead>
  <tbody>
    {{! Iterating over the database and populate genre list }}
    {{#each dataset}}
      <tr>
        <td>{{this.name}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>