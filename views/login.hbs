{{! Auhor: Karnasinh Gohil }}
{{! Boiler plate code has taken from this and then modified according to my requirement }}
{{! Boiler plte code link : https://mdbootstrap.com/docs/standard/forms/overview/ }}
<div class="col-4 container">
  {{! This is heading tage taking title variable value in it }}
  <h1 class="text-center margin32px">{{title}}</h1>

  {{! Show messages accordingly }}
  {{#if messages}}
    <div class="text-center alert alert-danger">Invalid Credentials</div>
  {{else}}
    <div class="text-center alert alert-info">Please enter your credentials
      below</div>
  {{/if}}

  <form method="post">

    <!-- Email input -->
    <div class="form-outline mb-4">
      <input name="username" id="username" class="form-control" required />
      <label for="username" class="form-label">Email address</label>
    </div>

    <!-- Password input -->
    <div class="form-outline mb-4">
      <input
        name="password"
        id="password"
        type="password"
        class="form-control"
        required
      />
      <label for="password" class="form-label">Password</label>
    </div>

    <!-- 2 column grid layout for inline styling -->
    <div class="row mb-4">
      <div class="col d-flex justify-content-center">
        <!-- Checkbox -->
        <div class="form-check">
          <input
            class="form-check-input"
            type="checkbox"
            value=""
            id="form2Example34"
            checked
          />
          <label class="form-check-label" for="form2Example34">
            Remember me
          </label>
        </div>
      </div>

      <div class="col">
        <!-- Simple link -->
        <a href="#!">Forgot password?</a>
      </div>
    </div>

    <div>
      <!-- Submit button -->
      <button type="submit" class="btn btn-primary btn-block mb-4">Sign in</button>
    </div>
    <div class="text-center margin32px">
      <p>or sign in with:</p>
      {{! github register link }}
      <a class="fab fa-github" href="/github">Github</a>
    </div>
    <div class="text-center">
      {{! register page link }}
      <p>Not a member? <a href="/register" title="Register">Register</a></p>
    </div>
  </form>
</div>